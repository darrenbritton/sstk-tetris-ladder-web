machine:
  node:
    version: 8.6.0

general:
  branches:
    ignore:
      - gh-pages

deployment:
  staging:
    branch: master
    commands:
      - npm run build
      - sh ./deploy-ghpages.sh .
