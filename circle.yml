machine:
  node:
    version: 8.6.0

general:
  branches:
    ignore:
      - gh-pages

deployment:
  staging:
    branch: master
    commands:
      - npm run build
      - git config --global user.email "deploy@circleci.com" && git config --global user.name "Circle CI"
      - npm run deploy
